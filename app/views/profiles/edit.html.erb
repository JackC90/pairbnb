<div class="jumbotron">
	<h2>Edit Profile</h2>
	  <%= form_for [@user, @profile] do |f| %>
	    <%= render "/profiles/form", f: f %>
	    <div class=submit>
	      <%= f.submit class: "btn btn-primary" %>
	      
	    </div>
	  <% end %>
	 <%= link_to "Back", user_profile_path(current_user, @profile) %>
</div>